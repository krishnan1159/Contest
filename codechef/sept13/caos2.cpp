#include<iostream>
#include<vector>
using namespace std;
int main()
{
	std::ios_base::sync_with_stdio(false);
	int t;
	cin>>t;
	while(t--)
	{
		int r,c,i,j,cnt;
		cin>>r>>c;

		vector< vector<int> >inp(r+1,vector<int>(c+1,-1)),left(r+1,vector<int>(c+1,-1)),right(r+1,vector<int>(c+1,-1)),top(r+1,vector<int>(c+1,-1)),bottom(r+1,vector<int>(c+1,-1));

	}
}
